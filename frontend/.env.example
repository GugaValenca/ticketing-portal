# Base URL of the backend API for local development.
# Use "localhost" (not 127.0.0.1) to match the backend's dev origin -
# the JWT auth cookies rely on both running on the same site locally.
VITE_API_BASE_URL=http://localhost:8001
